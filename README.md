# CS-progress
This repo is mainly focused on tracking progress of my C# learning.


1. Understanding Collections
I believe they are the core concepts for DSA and how you manipulate them and trying to come up with the fastest possible solution.
At first, I encountered Arrays which i have previous experience with it from C/C++ and Python nothing new in C# except the way of declaring it looks pretty much like Java
You can access them by using for loops which is the traditional way
Multi-Dimensional Arrays are just arrays inside each other but you access them by two for loops or more regarding the number of rows and columns that are there

Lists where things here get more flexible which i find very convinient to use instead of Arrays since they are generic and dynamic 
Declaring it is the same as vectors in C++
Count is a keyword that basically counts the number of items in a List.
Add() and Remove() and RemoveAt() to basically add and remove items from the list 
Contains() and IndexOf() checks if the list contains the passed item and the contains return boolean value while the index of returns the index of the found element and if not found returns -1
Sort() and Reverse() they are built-in sorting algorithms for the lists
the sort() sorts the list in an ascending order and the reverse() does the opposite thing of sort()

Dictionaries
